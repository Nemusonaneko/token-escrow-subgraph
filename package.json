{
  "name": "token-escrow",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ nemusonaneko/token-escrow",
    "create-local": "graph create --node http://localhost:8020/ nemusonaneko/token-escrow",
    "remove-local": "graph remove --node http://localhost:8020/ nemusonaneko/token-escrow",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nemusonaneko/token-escrow",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.36.1",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
